extends layout

block content
  h1= title
  hr
  ul
    each author in author_list
      li
        a(href=author.url) #{author.name}
        |  (#{author.formatted_DOB} - #{author.formatted_DOD})

    else
      li There are no authors.
